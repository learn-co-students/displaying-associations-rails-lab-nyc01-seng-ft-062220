<h1>Songs</h1>

<ul>
    <% @songs.each do |song| %>
    <li>
        <a href="<%= song_path(song) %>">
        <%= song.artist_name %>
        -
        <%= song.title %>


    </li>
    <% end %>
</ul>



<%#= link_to song.artist_name + ' - ' + song.title, artist_path(song) %>
<%# working code that the spec didn't like %>
